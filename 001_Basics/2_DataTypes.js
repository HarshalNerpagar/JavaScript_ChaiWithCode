/*
          DataTypes
 */

let name = "xyz"
let age = 42
let isGraterThan_50 = false

// IMP

let todayWeather = null // When we assign any variable with null we get its type as a Object
let nextKide; // When we create a variable but can't assign any value then it's and undefined type of object

console.log(typeof todayWeather)
console.log(typeof nextKide)
